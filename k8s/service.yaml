apiVersion: v1
kind: Service
metadata:
  name: nextix-service
  labels:
    app: nextix
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: nextix
